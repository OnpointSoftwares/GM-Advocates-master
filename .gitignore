# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# 📌 Dependencies
# Ignore node_modules directory (contains installed packages, not needed in Git)
node_modules/
.pnp

# 📌 Yarn Plug'n'Play file (not needed in version control)
.pnp.js

# 📌 Testing
# Ignore code coverage reports (generated during tests)
coverage/

# 📌 Production
# Ignore build folder (contains compiled code, not source code)
build/

# 📌 Miscellaneous
# Ignore macOS system file (automatically created)
.DS_Store

# 📌 Environment Variables
# Ignore local environment files (contain sensitive credentials & API keys)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 📌 Debugging Logs
# Ignore npm and Yarn debug logs (generated when errors occur)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
